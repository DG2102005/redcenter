# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

Vite + React 18 + TypeScript,单页应用,localStorage 本地持久化,无后端。运行入口 `npm run dev`。

## Users

麻将新手与进阶玩家,个人自学使用。核心工作:在无人指导的情况下,通过实际对弈 + 即时反馈来学习红中(癞子)推倒胡的正确打法、听牌判断与防守取舍。

## Product Purpose

提供一个可反复练习的红中百搭广东推倒胡麻将训练环境:1 人对 3 AI 对弈,系统在每步提供最优打牌建议、失误提醒、听牌/进张分析,对局结束后可收藏复盘并回到任意关键节点演绎不同打法。成功意味着玩家能清楚看到「为什么该这样打」,并在反复演绎中形成正确直觉。

## Positioning

把麻将从「娱乐游戏」重新定位为「可量化的训练工具」:不只给出打牌结果,还给出理由、事后复盘与分支推演——这是普通麻将游戏(只求胡牌)和真人牌局(无人讲解)都做不到的。核心机制是**人机对弈 + 每步即时辅助 + 任意节点演绎**。

## Operating Context

- 玩家坐在「南」位(人类座),对弈 3 个 AI(东/西/北)。
- 侧栏「信息」页展示牌局进度、积分(当轮/累计)、日志;「辅助」页展示当前最优打法、听牌张数与剩余张数。
- 支持:自摸胡、抢杠胡、明杠/暗杠/补杠实时计分(暗杠+6/明杠+3,其他家-2/-1)、撤销/重做、手牌牌型分解(手动点击切换间隔)、收藏本局并复盘、从复盘任意节点继续演绎。
- 对局中 AI 出牌有思考延迟;新摸的牌高亮置右,需玩家确认出牌。

## Capabilities and Constraints

- **红中(z5)为万能百搭牌**,可当任意牌参与面子或将,共 4 张,不可碰/杠。
- 胡牌型:标准推倒胡(4 面子 + 1 将);**仅支持自摸胡**,不点炮;支持抢杠胡(补杠时可被其他家抢)。
- 听牌/进张算法已支持红中补在顺子任意位置(起点/中间/末尾),如 `89条+红中` 可组成 `789条`。
- 出牌后按「万→筒→条→字」自动理牌,同牌成组。
- 座位逆时针行动:1南→0东→3北→2西。
- 积分持久化于 localStorage(累计/当轮两级,可分别清零)。
- 技术约束:纯前端单页,无后端、无账号系统;牌面为本地 PNG 素材。

## Brand Commitments

- 产品名:红中推倒胡麻将训练工具(标题:红中百搭 · 广东推倒胡)。
- 视觉基调已确定:深色牌桌 + 绿色桌面 + 木框 + 金色点缀(见现有 index.css),保持麻将桌沉浸感,不引入紫蓝 AI 渐变。

## Evidence on Hand

- 完整可运行代码:对弈引擎(gameEngine)、AI(aiEngine)、规则(rules)、胡牌判定(win)、听牌分析(skillEngine)、辅助决策(advisor)、复盘(review)、收藏(savedRounds)。
- 单元测试:`src/game/__tests__/`(win/skillEngine/shantenGrace/scoring/advisor)。
- 牌面素材:`public/tiles/*.png`。
- 无真实用户数据、无案例、无证言——未来不得虚构。

## Product Principles

1. 训练优先于娱乐:每个功能都要服务于「让玩家学会正确的打」,而非只求胡得快。
2. 反馈要有理由:给建议必须给理由与依据(如向听数、进张、危险度),不输出无解释的指令。
3. 允许试错与复盘:撤销、重做、任意节点演绎是核心训练手段,必须可靠。
4. 规则透明可校验:红中癞子、抢杠、计分等规则需与算法一致,且能被测试覆盖。
5. 界面让位于思考:视觉应辅助观察牌局(分组、高亮、间隔),不喧宾夺主。

## Accessibility & Inclusion

- 键盘可操作全部交互按钮(focus-visible 焦点环已实现)。
- 文字有清晰层级(主/次/弱),功能色用语义(金=强调、绿=听/成功、红=胡/危险)。
