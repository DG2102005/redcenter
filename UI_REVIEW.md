# 红中麻将助手 · UI 设计评审

> **评审对象**：`D:/TRAE/红中麻将助手/redcenter`（React 18 + TS + Vite，2036 行 index.css + 1092 行 quiz.css + 21 个组件）
> **评审方法**：源码精读 + Playwright 真实运行（1440×900 / 900×800 / 414×850 三档视口截图）+ DOM 抽样
> **评审人**：UI Designer
> **日期**：2026-09-08

---

## 0. 一句话结论

**这是一个"引擎强、界面弱"的项目**：游戏引擎、AI、复盘、辅助决策均已实现并有单测，但界面层让多数功能"看不到"——"答题"模块全量代码存在却**没有入口**，5 个面板组件写好了但**未被任何文件 import**，4 个被引用的 CSS 类在 quiz.css 中**重复定义两次**。在重写或重构前，建议先把"被隐藏的产品"梳理清楚，再谈视觉打磨。

按 P0→P2 修复后，界面可"至少能用且可被新人理解"；当前直接交给用户，**40% 的功能是不可发现的死代码**。

---

## 1. 关键发现（高优先级）

### 1.1 🚨 40% 的功能界面层不可达（信息架构断裂）

通过 `grep` 全仓验证，**以下模块用户根本无法触达**：

| 模块 | 文件 | 状态 | 行数 |
|---|---|---|---|
| 答题主入口 | `QuizHome.tsx`, `QuizCategorySelect.tsx` | ❌ 无任何 import | 362 + 140 |
| 答题游戏 | `QuizPlay.tsx` | ❌ 无任何 import | 251 |
| 自定义题库 | `CustomEditor.tsx`, `CustomList.tsx` | ❌ 无任何 import | 515 + 111 |
| 智能分析面板 | `SmartAnalysisPanel.tsx` | ✅ 已被 `QuizPlay` 引，但 QuizPlay 自身孤儿 | 117 |
| 高级建议面板 | `AdvisorPanel.tsx` | ❌ 0 引用 | 231 |
| 专家解释 | `ExpertPanel.tsx` | ❌ 0 引用 | 360 |
| 牌型分解 | `HandDecompositionPanel.tsx` | ❌ 0 引用，文件头注释"已废弃" | — |
| 策略库 | `StrategyLibPanel.tsx` | ❌ 0 引用 | 161 |
| 日志 | `LogPanel.tsx` | ❌ 0 引用 | — |
| 复盘详情 | `ReviewPanel.tsx` | ❌ 0 引用 | 93 |

**结果**：PRODUCT.md 第 14 行宣称提供"题库/答题/策略库/复盘"等 4 大训练模块，实际只有"对弈+智能模拟+复盘列表"3 个页面，且**"复盘"页面在无收藏时为 1 行空态**（截图 05）。

**改进建议**：
- 选项 A（推荐）：把这 10 个孤立组件逐一接入现有 UI，要么挂到导航，要么合并入对弈/智能模拟。
- 选项 B：明确"v1 不发布"标记后从仓库删除（`grep -L` 出 import 列表确认），减少认知负担。
- 在 `App.tsx` 顶部三 tab 上方加一个"ⓘ 训练模块"页面或下拉，列出所有功能与状态（已上线/即将上线/内测中）。

**预期效果**：从 5 个可见功能 → 8+ 个可见功能，新用户对"我能用它做什么"的理解度翻倍。

---

### 1.2 🚨 顶部导航的"智能模拟"名不副实

PRODUCT.md 第 14 行写的是"题库/答题"，但 `Simulator.tsx`（679 行）**只做"选牌推演"**（点牌池组手牌→AI 出牌→判定自摸）。无题库、无关卡、无计时、无评分。
`quiz.css` 第 39 行写的是 `.quiz-home`，但被 Simulator 复用做成了"训练场"，**用户预期与实际不符**。

**改进建议**：
- 把"选牌推演"改名为更准确的"🧪 牌面推演"或"🧪 进张分析器"，避免与"模拟考试"混淆。
- 若答题模块要被复活，在顶部加第 4 个 tab"📝 训练题库"，把 `QuizHome/QuizPlay/QuizCategorySelect` 接入。
- 在 Simulator 入口加 1 行副标题解释"这是什么 / 怎么用"。

**预期效果**：消除用户首屏困惑；新导航层次更清晰。

---

### 1.3 🚨 大量样式重复与魔法数字（设计系统崩坏）

quiz.css（1092 行）中存在 **17 组重复类名**（被覆盖，前面变成死代码）：

| 重复类名 | 出现次数 |
|---|---|
| `.quiz-toolbar-btn` | 9 |
| `.tile-pool-item` | 8 |
| `.quiz-option` | 8 |
| `.simulator-incoming-tile` | 5 |
| `.tile-pool-count` | 4 |
| `.simulator-drawn-tile` | 4 |
| `.quiz-modal-title` | 4 |
| `.mode-switch` | 4 |
| `.difficulty-tag` | 4 |
| `.custom-action-btn` | 4 |

`rgba(212,175,55,...)` 在 quiz.css 出现 **45 次**，而 `index.css:19` 已定义 `--gold-dim: rgba(212,175,55,0.28)` **几乎未被复用**。
`#e6c345`（L1087）和 `--gold: #d4af37`（index.css:17）**两个金色不一致**。
孤儿类：`.discard-incoming-*`（709-735）从未被任何组件使用。

**改进建议**：
1. 用脚本（`awk`/`jq`）找重复类，保留最后一个定义，删除前面。
2. 把 `rgba(212,175,55,0.X)` 全部改为 `var(--gold-dim)` 或新增 `--gold-faint` / `--gold-soft`。
3. 统一金色为 `--gold` 单变量，废弃 `#e6c345`。
4. 在 `src/styles/tokens.css`（新建）集中 token，再在 index.css / quiz.css 中只 `import` token 不重复定义。
5. 用 stylelint 配 `declaration-property-value-allowed-list` 禁止 `rgba(212,175,55, ...)` 形式。

**预期效果**：CSS 体量预计可从 3128 行减到 ~2000 行，主题色调整从改 45 处变成改 1 处。

---

## 2. 视觉与品牌

### 2.1 ✅ 做得好的地方

- **深色牌桌 + 金色点缀** 调性已建立（截图 01-02）：与 PRODUCT.md 第 45 行"不引入紫蓝 AI 渐变"承诺一致。
- **品牌色 token 已建**：`--felt/--wood/--gold/--gold-bright/--gold-dim/--accent/--text-dim`，覆盖面够。
- **状态色语义明确**：金=强调 / 绿=听牌 / 红=胡 / 蓝=信息，符合 PRODUCT.md 第 65 行"功能色用语义"。
- **顶导胶囊形态** + 选中态金色渐变 + scale(0.98) 按压反馈，整体手感统一（index.css:148-209）。

### 2.2 ❌ 视觉问题

| # | 问题 | 证据 | 优先级 |
|---|---|---|---|
| V1 | **欢迎页**右侧栏已显示空积分/局数/庄家，全是"0"和"-"，无意义噪声 | 截图 01 | P1 |
| V2 | 副标题"红中百搭·广东推倒胡"在空桌面正中央偏大字号，但与两侧留白比例不协调——牌桌顶部 1/3 空间被空占 | 截图 01/02 | P2 |
| V3 | **玩家牌桌正面（南）**没有"出牌堆"区域可见。AI 三家都有"手牌 13 张·副露 0 张"但**没有弃牌堆可视化**，人类玩家处也看不到自己的弃牌——产品宣称"撤销/重做/演绎"前提是能看见历史 | 截图 02 | **P0** |
| V4 | 牌桌区"推倒胡/红中百搭"水印在已开局后仍显示，与中区的弃牌池/骰子等中心元素视觉冲突 | 截图 02 中部"推倒胡"金色大字 | P2 |
| V5 | 顶部 H1 标题 18px + 🀄 emoji 颜色与金色对比不强（emoji 偏暗红），整体可读性 OK 但情绪弱 | 截图 02 顶部 | P3 |
| V6 | 副露区 `.meld-area` 在人类手牌上方一行，但**无视觉分组**与下方手牌分开，密集看 16 张牌 + 副露 0-3 组容易看错 | 截图 02 玩家(南) | P2 |
| V7 | 中心区"推倒胡/红中百搭"字体用了金色大字（Playfair-style）但与下方"剩余牌堆"等小数字没层级关系 | 截图 02 中心 | P3 |
| V8 | 玩家座位卡用 `rgba(0,0,0,0.32)` 暗背景，对比弱，14px 牌面在 170px 宽容器里上下挤 | 截图 02 三家 AI | P2 |
| V9 | "思考中…" 纯文字无 spinner 动画（PlayerSeat.tsx:33），AI 思考延迟期间用户感知不到 | 文字"思考中…" | P2 |

---

## 3. 信息架构（导航与页面结构）

### 3.1 当前导航

```
顶部：🀄 对弈 | 🧪 智能模拟 | 📚 复盘
侧栏（对弈时）：信息 | 🧭 辅助
```

### 3.2 痛点

- **"对弈"** 名字过于内行，**新用户第一眼不知道是什么**。PRODUCT.md 第 14 行说"1 人对 3 AI 对弈"——叫"训练对局"或"AI 对弈"更直接。
- **"复盘"** 在无收藏时是一行"对弈结束后点击'收藏本局'"，整个页面 80% 空白（截图 05）。**空态缺乏引导**：可以放"上一局回放""经典牌型""历史最佳"等内容占位。
- **"辅助"** 只在开局后才有内容（截图 03），开局前显示"开局后即可查看打牌建议"。但**没有解释什么是"辅助"**——对麻将新手来说，"辅助"可能指"出牌建议"，可能指"看哪张危险"，也可能指"复盘讲解"。
- 顶部三 tab 与侧栏两 tab **视觉权重等同**，但前者是页面级、后者是页面内 panel 级——用户难以一眼分辨。
- **无任何"学习路径"指引**。PRODUCT.md 第 56 行写"训练优先于娱乐"，但 UI 上没有任何 onboarding 步骤。

### 3.3 改进方案

```
顶部：🏠 主页 | 🀄 对弈训练 | 📝 答题 | 🧪 推演 | 📚 复盘 | ⚙ 设置
对弈训练页内右栏：📊 牌局信息 | 🧭 辅助决策 | 💡 听牌分析
```

具体：
1. **新增"主页"**：展示产品介绍、训练路径、最近战绩、推荐题。彻底解决"第一次进来不知道干嘛"。
2. **"对弈" → "对弈训练"**：明确是 AI 对战。放 1v3 AI 副标题"你坐南，对战 3 个 AI"。
3. **侧栏辅助** 加 tab 拆分：现在 AdvisorTab 一个组件塞 4-5 张推荐卡，应拆为"推荐/听牌/进张/危险" 4 个子 tab。
4. **每个空态加 2 步引导**："还没收藏过对局 ▶ 完成一局对弈 ▶ 点 💾 收藏本局 ▶ 在此查看"。

---

## 4. 交互流程

### 4.1 做得好的

- **出牌两步式**（HandRow.tsx:59-80）：首点置 `selected` 浮起 + 金框，再点同张确认。截图 02 中可见"点击选择要打出的牌"提示文字 + 提示徽章"分解牌型"。**避免误触，且与"分解牌型"教学动作分开**——是本项目交互最大的亮点。
- **撤销/重做** 浮在玩家区底部（App.tsx:208-229），不占牌桌。title 提示也到位。
- **新摸牌** 红色高亮"新"徽章 + 浮起（截图 02 玩家(南) 最右两张"中"/"发"红色边），区分清楚。

### 4.2 问题

| # | 问题 | 证据 | 优先级 |
|---|---|---|---|
| I1 | **AI 思考无 spinner**，"思考中…" 纯文字 | PlayerSeat.tsx:33 | P2 |
| I2 | **react/self 弹窗**（碰/杠/胡/过）以 `.action-panel` 内联在 human-seat 下方（App.tsx:193-206），**会推着 hand 向下移**——视觉上牌桌在"跳" | index.css .action-panel | P1 |
| I3 | **新摸牌提示"再次点击出牌"**（截图 02 玩家(南) 顶部红色"轮到你出牌"）与牌面选中状态高亮叠加，**信息冗余**：文字 + 金框 + 红框三处都在说"该你了" | 截图 02 | P3 |
| I4 | **两步出牌在快节奏对局中可能拖慢**——产品核心场景是"训练反应"，多一次点击影响节奏。建议增加"再次点击=出牌 / 双击=快速出牌" 提示 | HandRow.tsx:74-78 | P2 |
| I5 | **🧭 辅助**卡只能"推荐打 X"，**不能一键应用**——用户要点右侧卡再点手牌区，两边状态不同步（截图 03） | AdvisorTab.tsx | **P0** |
| I6 | **"分解牌型"按钮**位置和"再点出牌"在一起，**入口与出牌操作混在一起**——教学动作与游戏动作不应共用同一排 | 截图 02 玩家(南) | P1 |
| I7 | **顶部"开始游戏" / "开始新一局"** 在标题栏右侧金色 pill 内反复出现，**对老用户是冗余**——可改为"游戏"菜单收纳 | App.tsx:108-116 | P3 |
| I8 | **无任何快捷键**：撤销/重做/出牌/切换 tab 全部依赖鼠标。PRODUCT.md 第 64 行写"键盘可操作全部交互按钮"，**实测不达标**（53 张牌全为 div，无 tabIndex；撤销/重做能用但不直观） | DOM 抽查 0 个 role=tablist | **P0** |

---

## 5. 状态可见性

### 5.1 关键状态与可见性现状

| 状态 | 现状 | 问题 |
|---|---|---|
| 轮到谁 | AI seat 金色高亮（seat-active） + 文字"轮到你出牌"（App.tsx:181-183） | ✅ 良好 |
| 当前 phase | GameInfo 文字"出牌/未开始" | ⚠️ 文字小、位置远（要扫右栏） |
| 剩余牌数 | GameInfo:53 文字 + 局终可视化 | ⚠️ 在局中只是数字"83"，无视觉提示 |
| 听牌 | AdvisorTab 文字"听牌/N进听" | ⚠️ 桌面无任何听牌视觉提示，听牌时桌面应高亮可胡张 |
| 危险牌 | advisor.ts:284 已计算 `dangerLevel` | 🚨 **Tile 组件无 danger prop，桌面零提示**——这是核心安全提示 |
| 红中百搭 | Tile.tile-hongzhong 红色光晕 | ✅ 良好但与"危险红"颜色冲突 |
| 已选/新摸 | tile-selected 金色 / drawn-mark 红色 | ✅ 良好 |
| 收藏成功 | replayHint 顶部 2.5 秒飘字 | ⚠️ 飘字在金色标题栏易被忽略 |

### 5.2 改进方案

1. **听牌高亮**：当 `human.isTenpai === true`，**桌面所有手牌**中能听的张加金色下划线 + 角落小标"听"+ 候选张数。
2. **危险牌警示**：把 advisor.ts 算出的 `dangerLevel` 接到 `Tile` 组件 prop：
   - `dangerLevel: 0` → 无提示
   - `dangerLevel: 1` → 牌面右上角"!"橙标
   - `dangerLevel: 2` → 牌面红色虚线边框
3. **红中 vs 危险红 颜色冲突**：
   - 把红中改为"金色描边 + 中字 红色"（双重视觉编码），与危险色 `#ff5252` 分离。
4. **阶段指示器**：在 human-seat 顶加 5 段进度条（出牌→碰杠→听牌→自摸→结束），让用户一眼看清阶段。
5. **局数与剩余牌视觉化**：剩余 50 张以上→绿、20-50→黄、<20→红。局数从文字改为徽章圆点。

---

## 6. 响应式

### 6.1 现状

- 断点：仅 `prefers-reduced-motion` + `max-width: 1400/1200/1000`（index.css:1993/2006/2015/2025），**quiz.css 0 个断点**。
- 固定尺寸：side-panel 272px、player-seat 170px、seat-zone 180px、tile 46px（人类手牌 ~736px 宽）。

### 6.2 实测问题

| 视口 | 行为 | 问题 |
|---|---|---|
| 1440×900（桌面） | ✅ OK | 截图 02 |
| 900×800 | 桌面布局塌成单列 + 左右 AI 座位隐藏 | 截图 06 顶部 16 张手牌 + 信息栏塌到下方，但手牌行没变窄——视觉上仍有横向溢出风险 |
| 414×850（移动） | 🚨 **标题换行截断、顶部 3 tab 被压成 3 个 80px 胶囊、玩家手牌 16 张只显示 7 张**——**完全不可用** | 截图 07 |

### 6.3 改进方案

1. **新增断点**（index.css + quiz.css 都加）：
   - `max-width: 720`：手牌改 36px、手牌行允许 `overflow-x: auto`、侧栏从右侧栏改底部抽屉。
   - `max-width: 480`：顶部导航改为 hamburger 菜单、3 tab 收入侧滑面板、AI 三家改"摘要行"（只显示名字/牌数/思考状态）。
2. **所有 tile 容器加 `min-width: 0` + `flex-wrap: wrap`**，避免横向滚动。
3. **人类手牌 16 张 ≤ 360px 时改纵向 4×4 网格**（类似真实手机麻将游戏）。
4. **不要让标题 H1 在窄屏截断**：h1 加 `white-space: nowrap; overflow: hidden; text-overflow: ellipsis`，或在 <600px 简化为 🀄 + "红中"。

---

## 7. 无障碍（WCAG AA 评估）

通过 Playwright 实际 DOM 抽样：
```
tileCount: 53, tileTag: DIV, tablist: 0, ariaSelected: 0,
ariaLive: 0, ariaLabel: 0, buttons: 8, divClick: 92
```

**问题严重度**：

| # | 问题 | 证据 | WCAG | 优先级 |
|---|---|---|---|---|
| A1 | **53 张牌全为 div** 而非 button，**键盘无法出牌** | Tile.tsx:23 | 2.1.1 | **P0** |
| A2 | 顶部 3 tab + 侧栏 2 tab **无 role="tablist"/aria-selected** | App.tsx:88-107, 238-243 | 4.1.2 | P1 |
| A3 | **0 个 aria-live 区域**——AI 思考、阶段变化、收藏成功等动态信息对屏幕阅读器不可感知 | 全仓 grep | 4.1.3 | P1 |
| A4 | **0 个 aria-label**——AI 座位、人类座位、tile 全靠 title/文字 | 全仓 grep | 1.1.1 | P1 |
| A5 | **仅有 8 个 button**——绝大多数交互元素是 div+onClick | DOM 抽查 | 4.1.2 | **P0** |
| A6 | **seat-stats 10px 文字 + 颜色 rgb(185,176,156)**——与深绿底对比度约 4.4:1，勉强过 AA 但接近边界 | 实测 | 1.4.3 | P2 |
| A7 | **focus-visible 仅覆盖 button/a/.nav-btn/.start-btn/.action-btn**（index.css:86,168,197），未覆盖 .quiz-toolbar-btn/.quiz-option/.quiz-mode-card | 实测 | 2.4.7 | P1 |
| A8 | **没有 Skip Link**（"跳到主内容"） | 无 | 2.4.1 | P2 |
| A9 | **没有错误反馈给屏幕阅读器**（如"不能出红中"等规则错误） | 看不到 | 3.3.1 | P2 |

### 改进清单

1. **A1 / A5**：把 `Tile` 改为 `<button class="tile">`，根据状态加 `aria-disabled` / `aria-label="一万" aria-pressed={isSelected}`。
2. **A2**：tab 容器加 `role="tablist"`，每个 tab 加 `role="tab" aria-selected={isActive}`。
3. **A3**：在 `.app-header` 下方加 `<div aria-live="polite" className="sr-only">{phaseMessage}</div>`。
4. **A4**：人类座位 + 牌 + 按钮全部加 `aria-label`。
5. **A7**：把 `:focus-visible` 全局化，扩展到 `.tile, .quiz-option, .quiz-mode-card`。
6. **A6**：把 `.seat-stats` 提到 12px，颜色改 `var(--text-dim) → #d5cdb5`。

---

## 8. 设计系统健康度

### 8.1 Token 体系

✅ 已建：`--felt/--wood/--tile-bg/--accent/--gold/--gold-bright/--gold-dim/--text/--text-dim/--text-muted/--success/--danger/--info/--purple/--shadow-{sm,md,lg}`

❌ 缺失：
- **间距 token** 只有 `--space-1:4/--space-2:8/--space-3:12/--space-4:16/--space-6:24/--space-8:32/--space-12:48/--space-16:64`（index.css 内未定义）——实际**未真正建立**，组件 CSS 里到处是 `8px/12px/16px/24px/32px` 硬编码。
- **字号 token** 同样未建立：13/14/16/18 等直接写在各处。
- **圆角 token** 已建但用得不全。
- **动效 token** 已建 `--transition`，但 quiz.css 中大量 `transition: all 0.2s ease` / `0.3s ease` 硬编码，绕开 token。

### 8.2 建议

新建 `src/styles/tokens.css`（在 `main.tsx` 中先 import）：

```css
:root {
  /* 间距 */
  --space-1: 4px; --space-2: 8px; --space-3: 12px; --space-4: 16px;
  --space-5: 20px; --space-6: 24px; --space-8: 32px; --space-10: 40px;
  --space-12: 48px; --space-16: 64px; --space-20: 80px;

  /* 字号 */
  --text-xs: 11px; --text-sm: 12px; --text-base: 14px;
  --text-md: 15px; --text-lg: 16px; --text-xl: 18px;
  --text-2xl: 22px; --text-3xl: 28px; --text-display: 36px;

  /* 行高 */
  --leading-tight: 1.25; --leading-normal: 1.5; --leading-loose: 1.75;

  /* z-index */
  --z-base: 0; --z-elevated: 10; --z-sticky: 20;
  --z-overlay: 100; --z-modal: 200; --z-toast: 300;
}
```

并加 stylelint 规则禁止在组件 CSS 里写裸数字 + px。

---

## 9. 改进优先级清单

### P0（不做完不能发布给真实用户）

1. **接入或删除孤立组件**（§1.1）——10 个组件中至少要让"题库/复盘详情"有入口。
2. **人类弃牌堆可见化**（§2.2 V3）—— 撤销/重做的前提是看到历史。
3. **辅助推荐一键应用**（§4.2 I5）——侧栏"打 X"应能直接点出。
4. **键盘出牌**（§7 A1）—— 53 张牌改为 `<button>`，按 1-9 可选/出。
5. **加 Skip Link + 全局 focus-visible 扩展**（§7 A7-A8）。

### P1（可显著提升体验）

6. 命名修正："智能模拟"→"牌面推演"、新增"答题" tab（§1.2、§3.3）。
7. 去掉欢迎页右侧栏的 0/-/未开始 噪声（§2.2 V1）。
8. react/self 弹窗改为覆盖层（modal 形式）避免牌桌跳（§4.2 I2）。
9. "分解牌型"教学按钮与出牌操作分两行（§4.2 I6）。
10. 阶段指示器 5 段进度条（§5.2）。
11. 危险牌与听牌张在桌面高亮（§5.2）。
12. CSS 重复类去重（§1.3）。
13. quiz.css 与 index.css token 统一（§8.1）。
14. 全面加 aria-label/role="tablist"（§7 A2-A4）。

### P2（打磨细节）

15. 移动端断点扩展到 480/720（§6.3）。
16. AI 思考改 spinner（§2.2 V9、§4.2 I1）。
17. 出牌加双击/快捷键支持（§4.2 I4）。
18. 红中 vs 危险红颜色解耦（§5.2）。
19. 字号/间距 token 体系建立（§8.2）。
20. 顶部"开始游戏"按钮收入菜单（§4.2 I7）。
21. 复盘空态加引导动画（§3.2）。

### P3（视觉精致度）

22. 标题 H1 emoji 颜色强化。
23. 牌桌中央"推倒胡"水印在开局后淡出。
24. 玩家座位卡背景对比度提升。
25. 收藏成功提示改为右下 toast（更不打扰）。

---

## 10. 关键数据回顾（来自实测）

| 指标 | 当前 | 目标 |
|---|---|---|
| 死代码组件 | 10 个 | 0（接入或删除） |
| CSS 行数 | 3128 | ≤ 2000 |
| 重复类定义 | 17 组 | 0 |
| 硬编码 `rgba(212,175,55, ...)` | 45 处 | ≤ 5 |
| `<div class="tile">` 非交互元素 | 53 个 | 0（改 button） |
| 顶部 tab 焦点状态 | 0 个 aria-selected | 3 个 |
| 移动端（414px）可用性 | 不可用 | 核心功能可点 |
| 听牌/危险牌可视化 | 0 | 至少听牌张 + 危险张 |

---

## 11. 一句话给 Dark（项目作者）

**这个产品的"发动机"已经装好了（引擎/AI/复盘/算法都有测试），但"方向盘和仪表盘"还停在 0.5 版本——大量功能写好了但找不到，按钮不像按钮，键盘出不了牌，移动端挤爆。** 接下来最该做的不是继续加功能，而是把现有的"挂到界面上"、把 CSS 重复类清理掉、把 Tile 改成真按钮、并在桌面与移动各跑一次真实对局录像，标出每一处"卡住"的地方。

---

*附：本次评审使用的 7 张截图存于 `D:/WorkBuddy/2026-09-08-07-42-47/.uicap/shots/`，对应章节已引用截图编号（01-07）。*
